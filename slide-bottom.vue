<script setup>
// The bottom-right pill (slide number + giornata_slug) previously rendered
// here is now handled by <SessionChrome /> via global-top.vue. That combines
// the page counter with the inherited session metadata in a single editorial
// chrome element. This file is kept as a no-op so existing deck overrides
// (decks that supply their own slide-bottom.vue) are not disturbed by an
// absent theme file.
</script>

<template></template>
